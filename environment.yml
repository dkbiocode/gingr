name: gingr-build
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - qt=5.15
  - protobuf
  - capnproto
  # Build tools - Unix only (Windows will use pre-installed tools)
  - autoconf  # [not win]
  - automake  # [not win]
  - make      # [not win]
  # Platform-specific packages will be installed in the workflow
