name: gingr-build
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - qt=5.15
  - protobuf
  - capnproto
  # Note: Build tools (autoconf, automake, make) are installed per-platform in workflow
